/**
 * @startsWith -> Se a string começa com o caracteres digitado, ou palavra
 * @return é um boolean
 *
 */

const tech = 'Node.js';
const startsWith = tech.startsWith('No');
console.log({ startsWith });
